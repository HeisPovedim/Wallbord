/**
 * Standard DigitalSkynet version.json response - for Kapitan and Valera.
 */
interface Version {
  hash: string;
  branch: string;
  buildDate: string;
  deployDate: string;
}

export default Version;
